#
# Run starship as prompt
# Fast prompt written in rust that is easy to configure
# https://starship.rs

# custom config
export STARSHIP_CONFIG=~/dotfiles/zsh/.zsh/starship.toml

eval "$(starship init zsh)"
